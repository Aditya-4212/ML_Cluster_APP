[theme]
base = "dark"                    # Default theme (can be overridden by toggle)
backgroundColor = "#07080f"
secondaryBackgroundColor = "#0d0e1a"
textColor = "#e2e4f0"
font = "monospace"

[server]
maxUploadSize = 50
headless = true
